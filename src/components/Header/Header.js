import React from 'react';
// import PropTypes from 'prop-types';

function Header(props) {
  return (
    <header>
      <nav></nav>
    </header>
  );
}

Header.propTypes = {};

export default Header;
